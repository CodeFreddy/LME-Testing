# This project currently uses only Python standard library modules.
# No third-party pip packages are required at this stage.
# Tested with Python 3.14.0; recommended minimum version is Python 3.11.
#
# Setup example:
#   python -m venv .venv
#   .\.venv\Scripts\activate
#   pip install -r requirements.txt
